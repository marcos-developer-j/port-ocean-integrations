[tool.poetry]
name = "github-projects"
version = "0.1.0"
description = "GitHub Projects V2 Ocean integration for Port"
authors = ["Port Iniciatives"]

[tool.poetry.dependencies]
python = ">=3.12,<3.14"
port_ocean = {version = ">=0.18.0,<1.0.0", extras = ["cli"]}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
